export declare function encodedPass(rawPassword: string): string;
export declare function comparePass(rawPassword: string, hash: string): boolean;
export declare function encodedToken(token: string): string;
export declare function compareToken(token: string, hash: string): boolean;
